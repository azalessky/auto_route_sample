name: auto_route_sample
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  auto_route: ^10.2.3
  auto_route_generator: ^10.2.6
  build_runner: ^2.10.4

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true